"""Web crawler submodules.

`crawler.py`             — async httpx + bs4 + markdownify BFS crawler
`playwright_crawler.py`  — JS-rendered fallback, off by default
`pipeline.py`            — `run_crawl_job(job_id)` background-task entrypoint
"""
